name: 🚀 Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy pos-tienda
    runs-on: ubuntu-latest

    steps:
      - name: 🔌 Conectar al VPS y desplegar
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            echo "📦 Actualizando código..."
            cd /var/www/pos-tienda
            git pull origin main

            echo "📥 Instalando dependencias..."
            npm ci

            echo "🔨 Compilando..."
            npm run build

            echo "♻️  Reiniciando servicio..."
            /usr/lib/node_modules/pm2/bin/pm2 reload pos-tienda || /usr/lib/node_modules/pm2/bin/pm2 start ecosystem.config.js

            echo "✅ Deploy completado"
